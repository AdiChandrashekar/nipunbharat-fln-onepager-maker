/** Per-document selection limits (see SPEC.md "Limits"). */
export const MAX_GROUPS = 8;
export const MAX_STRATEGIES = 24;
/** Strategies brought in when a whole competency is added: its top N by frequency_count. */
export const COMPETENCY_TOP_N = 3;
